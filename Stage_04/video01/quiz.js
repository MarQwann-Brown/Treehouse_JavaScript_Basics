var answer = prompt("What programming language is named after a gem?");
if (answer.toUpperCase() === "RUBY") {
    document.write("That is correct.");
} else {
    document.write("That is incorrect");
}
